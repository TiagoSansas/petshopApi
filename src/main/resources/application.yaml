spring:
  application:
    name: petshoop

  datasource:
    url: jdbc:postgresql://172.18.218.241:5433/postgres
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true
